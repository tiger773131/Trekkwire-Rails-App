<div class="container mx-auto p-4">
  <div class="my-8">
    <h1><%= t(".title") %></h1>

    <div data-controller="user-map" data-action="map-loaded@window->address#initGoogleMap" class="grid grid-cols-2">
      <% if current_account.customer_type == "traveler" %>
          <div class="col-span-1">test</div>
          <div data-user-map-target="map"  class="col-span-1" style="height:400px;width:400px"></div>
      <% else %>
        <div class="mb-2">This is the homepage for guides.</div>
      <% end %>
    </div>
  </div>
</div>