<div class="px-4 sm:px-6 lg:px-8">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-base font-semibold leading-6 text-gray-900">Tours</h1>
      <p class="mt-2 text-sm text-gray-700">A list of all the Tours you are currently offering.</p>
    </div>
    <div class="mt-4 sm:ml-16 sm:mt-0 sm:flex-none">
      <%= link_to "New Tour", new_tour_path, class: "block rounded-md bg-indigo-600 px-3 py-2 text-center text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600" %>
    </div>
  </div>
  <div class="mt-8 flow-root">
    <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <table class="min-w-full divide-y divide-gray-300">
          <thead>
          <tr>
            <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-0"><%= sortable @tours, :title, "Title", class: "text-gray-700 mr-1" %></th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Scheduled</th>
            <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-0"><%= sortable @tours, :price, "Price", class: "text-gray-700 mr-1" %></th>
            <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-0">Duration</th>
            <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-0">Tagline</th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900 ">Description</th>
            <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-0">
              <span class="sr-only">Edit</span>
            </th>
          </tr>
          </thead>
          <tbody class="divide-y divide-gray-200">
          <% current_account.tours.each do |tour| %>
            <tr>
              <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-gray-900 sm:pl-0"><%= link_to tour.title.truncate(20), tour_path(tour) %></td>
              <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500"><%= tour.scheduled_tours.count %></td>
              <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500"><%= tour.price %></td>
              <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500"><%= tour.duration %></td>
              <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500"><%= tour.tagline.truncate(30) if tour.tagline %></td>
              <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500"><%= tour.description.truncate(40) if tour.description %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
